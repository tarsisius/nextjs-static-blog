export const folder = "_posts";
export const dir = require("path").join(process.cwd(), `public/${folder}`);
export const siteName = "Blog";
export const siteDesc = "Just the best i can. Hope it's a little bit useful.";
export const perPage = 6;
